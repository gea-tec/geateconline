baseURL = "https://gea-tec.com/"
languageCode = "en-us"
locale = "es"
title = "GEATEC Suelos y Hormigones"
disableKinds = ["taxonomy", "taxonomyTerm", "section"]
# We do redirects via Netlify's _redirects file, generated by Hugo (see "outputs" below).
disableAliases = true
